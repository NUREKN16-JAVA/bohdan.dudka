<project name="usermanagment" basedir="." default="test">
    <property name="built-dir" value="${basedir}/out"/>
    <property name="classes-dir" value="${built-dir}/classes"/>
    <property name="main-dir" value="${basedir}/src/main/java"/>
    <property name="test-java-dir" value="${basedir}/src/test/java"/>

    <target name="clean">
        <delete dir="${built-dir}" failonerror="no"/>
    </target>

    <target name="init" depends="clean">
        <mkdir dir="${built-dir}"/>
        <mkdir dir="${classes-dir}"/>
    </target>

    <target name="compile" depends="init">
        <javac srcdir="${main-dir}" destdir="${built-dir}"/>
    </target>

    <target name="test-compile" depends="init">
        <javac srcdir="${test-java-dir}" destdir="${built-dir}">
            <classpath>
                <path id="org.junit" location="/home/bohdan/IntellijIDEAProjects/usermanagment/lib/junit-4.10.jar"/>
            </classpath>
        </javac>
    </target>

    <target name="test" depends="compile, test-compile">
        <junit>
            <classpath>
                <pathelement location="${built-dir}"/>
                <fileset dir="./lib">
                    <include name="**/junit-4.10.jar"/>
                    <include name="**/hamcrest-all-1.3.jar"/>
                </fileset>
            </classpath>
            <test name="ua.nure.kn.dudka.usermanagment.UserTest"/>
            <formatter type="plain"/>
        </junit>
    </target>
</project>